{
  "name": "connectivity-state",
  "version": "1.0.0",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "An element that detects online/offline states and informs about it other compopnents",
  "devDependencies": {
    "polymer-cli": "^0.17.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": false,
  "main": [
    "connectivity-state.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "lint": "node_modules/.bin/polymer lint connectivity-state.html",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"connectivity-state:local-test\"",
    "test": "node_modules/.bin/polymer test",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/connectivity-state.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/connectivity-state/issues",
    "email": "arc@mulesoft.com"
  }
}
